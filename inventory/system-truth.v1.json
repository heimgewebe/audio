{
  "schema_version": 1,
  "kind": "audio_system_truth_contract",
  "authority_order": [
    "live read-only observation",
    "explicit human physical observation",
    "validated private laboratory evidence",
    "repository plan and defaults"
  ],
  "bound_components": [
    "inventory/signal-path.v1.json",
    "inventory/physical-facts.v1.json",
    "inventory/physical-verification.v1.json",
    "inventory/laboratory-gates.v1.json",
    "profiles/audio-profiles.v1.json",
    "profiles/reference-levels.v1.json",
    "private physical state digest",
    "private laboratory state digest and validated gate projection"
  ],
  "required_observations": [
    "normalized MOTU and Roland presence",
    "PipeWire rate, quantum, defaults and canonical graph fingerprint",
    "PipeWire, WirePlumber, Mopidy and EasyEffects states and limits",
    "audio recorder, plugin host, playback and creative-runtime process fingerprints",
    "recent XRun-like journal line count and digest",
    "filesystem free space and bounded audio state usage",
    "kernel, PipeWire, WirePlumber and Mopidy versions",
    "resolved, invalidated and unresolved laboratory gates"
  ],
  "completion_gates": [
    "single-truth-model",
    "host-read-boundary",
    "safe-listening-calibration",
    "voice-reference",
    "latency-xrun-baseline",
    "qobuz-rate-proof",
    "rate-resampling-policy",
    "managed-plugin-host-proof",
    "device-loss-baseline",
    "runtime-storage-observation",
    "drift-contract"
  ],
  "rules": [
    "unknown physical state remains unknown",
    "a PipeWire buffer period is not round-trip latency",
    "absence of recent XRun-like log lines is not an XRun-free proof",
    "service activity is not profile readiness",
    "all report commands are argv-only, memory-bounded and read-only denylist checked",
    "raw process arguments, stdout, stderr and journal lines are never persisted",
    "private reports use no-follow directory descriptors, bounded reads and atomic mode-0600 writes",
    "derived aggregates and fingerprints are recomputed during verification",
    "the report timestamp is covered by report_sha256",
    "standalone hashes provide integrity checks, not authenticity; authenticity requires an external digest pin or signature",
    "drift identifies catalog-exact remeasurement gates but never applies a profile"
  ]
}
